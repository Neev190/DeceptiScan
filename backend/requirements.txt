# Core Flask dependencies
flask==3.0.0
flask-cors==4.0.0
flask-sqlalchemy==3.1.1
flask-jwt-extended==4.6.0

# Database
psycopg2-binary==2.9.9
alembic==1.13.0
flask-migrate==4.0.7

# Cache
redis==5.0.1

# Authentication
python-jose[cryptography]==3.3.0
bcrypt==4.1.1

# Validation
pydantic==2.5.0

# ML dependencies
# Note: transformers has a Windows long-path issue with default pip install location.
# Install to a short path: pip install transformers tokenizers --target D:\\pylibs --no-deps
# The D:\\pylibs path is added to sys.path via deceptiscan_pylibs.pth
transformers>=4.36.2
torch>=2.2.0
sentencepiece==0.1.99
numpy>=1.26.0
# Training and evaluation (run ml_training/train.py once to produce checkpoint)
datasets==2.18.0
scikit-learn==1.4.2
evaluate==0.4.1
accelerate==0.29.3

# Utilities
python-dotenv==1.0.0
requests==2.31.0
gunicorn==21.2.0

# Retrieval layer (Phase 2 — pgvector similarity search)
sentence-transformers>=2.7.0
pgvector>=0.3.0

# File Storage / Media
cloudinary>=1.36.0